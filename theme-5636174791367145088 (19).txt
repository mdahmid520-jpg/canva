<!DOCTYPE html>
<html lang="en">
<head>  
  <meta charset="UTF-8"/>  
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>  
  <title>Canva Pro Team</title>  

  <!-- ✅ AdExtra Script -->
  <script async src="https://partner.adextra.io/jt/e51576d6ae62ae9e26fcab127d3995a827b7c8f9.js"></script>

  <style>
    :root {
      --primary: #6200ea;
      --secondary: #03dac5;
      --light: #ffffff;
      --dark: #121212;
      --card-bg: #1f1f1f;
      --text-color: #ffffff;
      --ad-bg: #222;
      --ad-item-bg: #333;
      --border-color: #444;
    }

    body.light-mode {
      --dark: #ffffff;
      --card-bg: #ffffff;
      --text-color: #000000;
      --ad-bg: #f9f9f9;
      --ad-item-bg: #f1f1f1;
      --border-color: #dddddd;
      background: #ffffff !important;
    }

    * {
      margin: 0; padding: 0; box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, sans-serif;
    }

    body {
      background: linear-gradient(135deg, var(--dark), #1a1a2e);
      color: var(--text-color);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      transition: all 0.4s ease;
      padding: 20px;
      text-align: center;
    }

    .container {
      width: 100%;
      max-width: 500px;
      background: var(--card-bg);
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
      overflow: hidden;
      padding-bottom: 20px;
      transition: all 0.4s ease;
      position: relative;
    }

    .header {
      background: linear-gradient(90deg, var(--primary), var(--secondary));
      padding: 25px 20px;
      text-align: center;
    }

    .header h1 { font-size: 30px; color: #fff; }
    .author { margin-top: 10px; font-size: 12px; color: #f5f5f5; }
    .content { padding: 25px; }

    .progress-container {
      width: 100%;
      background: #333;
      border-radius: 12px;
      margin-bottom: 25px;
      overflow: hidden;
      height: 30px;
      display: flex;
      border: 1px solid var(--border-color);
    }

    .step-box {
      flex: 1;
      text-align: center;
      line-height: 26px;
      font-size: 14px;
      font-weight: bold;
      color: #fff;
      background-color: #444;
      transition: background 0.3s ease-in-out;
    }

    .step-box.done-1 { background-color: red; }
    .step-box.done-2 { background-color: orange; }
    .step-box.done-3 { background-color: limegreen; }
    .step-box.done-4 { background-color: blueviolet; }
    .step-box.done-5 { background-color: deepskyblue; }

    .buttons {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    .btn-3d {
      padding: 16px 20px;
      border: none;
      border-radius: 15px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      color: var(--light);
      background: var(--primary);
      transition: transform 0.2s ease, background 0.3s ease;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
    }
    .btn-3d:hover { transform: translateY(-2px); }
    .btn-small { padding: 10px 15px; font-size: 14px; }

    .ad-notification {
      position: fixed;
      top: 20px; left: 50%;
      transform: translateX(-50%);
      background: rgba(0, 0, 0, 0.8);
      color: var(--light);
      padding: 15px 30px;
      border-radius: 10px;
      z-index: 1000;
      display: none;
    }

    .ads-section {
      margin-top: 30px;
      padding: 15px;
      background: var(--ad-bg);
      border-radius: 15px;
      border: 1px solid var(--border-color);
    }

    .ad-item {
      background: var(--ad-item-bg);
      border-radius: 10px;
      padding: 10px 15px;
      margin-bottom: 10px;
    }
    .ad-item a {
      color: var(--text-color);
      text-decoration: none;
      font-size: 14px;
      display: block;
    }
    .ad-item a small { color: #888; font-size: 12px; }

    #mode-toggle {
      position: absolute;
      top: 5px;
      right: 5px;
      padding: 4px 5px;
      background: transparent;
      color: #fff;
      border: none;
      border-radius: 100px;
      cursor: pointer;
      font-size: 16px;
      z-index: 1100;
    }
  </style>
</head>
<body>
  <div class="ad-notification" id="ad-notification"></div>

  <div class="container">
    <button id="mode-toggle" onclick="toggleMode()">🌙</button>
    <div class="header">
      <h1>Canva Pro Team</h1>
      <p class="author">📢 After watching 5 ads, the Canva Pro button will be unlocked. Then click again to open Canva Pro.</p>
    </div>
    <div class="content">
      <div class="progress-container">
        <div class="step-box" id="step1">Step 1</div>
        <div class="step-box" id="step2">Step 2</div>
        <div class="step-box" id="step3">Step 3</div>
        <div class="step-box" id="step4">Step 4</div>
        <div class="step-box" id="step5">Step 5</div>
      </div>

      <div class="buttons">
        <button class="btn-3d" id="canva-pro-btn" onclick="handleClick()">🔒 Watch Ads to Unlock Canva Pro</button>
        <button class="btn-3d btn-small" onclick="openCommonLink('https://t.me/+degfJyrRbIc1YzVl')">Join Channel</button>
        <button class="btn-3d btn-small" onclick="openCommonLink('https://t.me/+7gb19q80la81ZGVl')">Premium Apps</button>
      </div>

      <div class="ads-section">
        <div class="ad-item">
          <a href="https://t.me/+sBrvqGQ6eeFhZjk1" target="_blank">
            💰 Earn Daily from Airdrops
            <small>Join now to claim free crypto rewards</small>
          </a>
        </div>

        <!-- ✅ AdExtra Ad Block -->
        <div id="e51576d6ae62ae9e26fcab127d3995a827b7c8f9"></div>
      </div>
    </div>
  </div>

  <script>
    const CANVA_LINK = "https://www.canva.com/brand/join?token=hVKc0Gyq4UN5xGGtwzQDRQ&brandingVariant=edu&referrer=team-invite";
    let adCount = 0, adsCompleted = false;

    window.addEventListener("DOMContentLoaded", () => {
      const savedMode = localStorage.getItem("themeMode");
      if (savedMode === "light") {
        document.body.classList.add("light-mode");
        document.getElementById("mode-toggle").textContent = "☀️";
      }
    });

    function handleClick() {
      const button = document.getElementById('canva-pro-btn');
      if (adsCompleted) {
        openCommonLink(CANVA_LINK);
        return;
      }
      showAds();
    }

    function openCommonLink(url) {
      window.location.href = url;
    }

    function showAds() {
      const button = document.getElementById('canva-pro-btn');
      let countdown = 3;
      button.disabled = true;
      button.style.background = 'gray';
      const interval = setInterval(() => {
        button.textContent = `Please wait... ${countdown} sec`;
        countdown--;
        if (countdown < 1) {
          clearInterval(interval);
          showAd();
        }
      }, 1000);
    }

    function showAd() {
      const button = document.getElementById('canva-pro-btn');
      // ⚠️ AdExtra SDK সরাসরি Promise দেয় না, তাই আমরা simulate করব
      adCount++;
      updateSteps();

      if (adCount >= 5) {
        adsCompleted = true;
        button.textContent = "🎉 Click to Open Canva Pro";
        button.style.background = "green";
        button.disabled = false;
      } else {
        resetButton();
      }
    }

    function updateSteps() {
      if (adCount >= 1) document.getElementById('step1').classList.add('done-1');
      if (adCount >= 2) document.getElementById('step2').classList.add('done-2');
      if (adCount >= 3) document.getElementById('step3').classList.add('done-3');
      if (adCount >= 4) document.getElementById('step4').classList.add('done-4');
      if (adCount >= 5) document.getElementById('step5').classList.add('done-5');
    }

    function resetButton() {
      const button = document.getElementById('canva-pro-btn');
      setTimeout(() => {
        button.textContent = "🔒 Watch Ads to Unlock Canva Pro";
        button.disabled = false;
        button.style.background = "var(--primary)";
      }, 500);
    }

    function toggleMode() {
      const body = document.body;
      const btn = document.getElementById('mode-toggle');
      body.classList.toggle('light-mode');

      if (body.classList.contains('light-mode')) {
        btn.textContent = '☀️';
        localStorage.setItem("themeMode", "light");
      } else {
        btn.textContent = '🌙';
        localStorage.setItem("themeMode", "dark");
      }
    }
  </script>
</body>
</html>
